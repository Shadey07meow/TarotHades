package object.statics;

public enum Relic {

    // On death, resurrect once with 50 % of max HP.
    DEATH,

    // Permanent 8 % flat boost to all base stats (HP, ATK, DEF, SPD).
   
    
    MAGICIAN,

    // Starting max HP is set to 15 hearts.
    THE_EMPRESS
}
